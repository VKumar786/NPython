
import shutil
import os

for i in range(1, 45):
    # try:
    #     os.remove(f"tut{i}.py")
    # except Exception as e:
    #     pass

    # os.mkdir(f"tut{i}")

    # source_file = f"tut{i}.py"
    # destination_folder = f"tut{i}/"

    # shutil.copyfile(source_file, os.path.join(destination_folder + source_file))
    pass
